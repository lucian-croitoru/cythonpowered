# Dependencies for building / installation
Cython>=3.0.0
setuptools==74.1.3
wheel==0.43.0
more-itertools==10.0.0
packaging==24.0

# Dependencies for utils (--benchmark | --list)
prettytable>=3.0.0
psutil>=6.0.0
py-cpuinfo>=9.0.0
pandas>=2.0.0

# Testing
pytest>=8.0.0

# Publishing to pypi
twine>=5.0.0
